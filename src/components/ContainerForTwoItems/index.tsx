import * as Styled from './styles';
import { ContainerForTwoItemsProps } from './ContainerForTwoItemsProps';

export const ContainerForTwoItems = ({ children }: ContainerForTwoItemsProps) => {
  return <Styled.ContainerForTwoItems>{children}</Styled.ContainerForTwoItems>;
};
